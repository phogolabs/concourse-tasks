---
platform: linux

image_resource:
  type: docker-image
  source: {repository: phogo/blog, tag: 'latest'}

inputs:
- name: concourse-tasks
- name: blog-source
- name: blog-website

params:
 BLOG_CONFIG:
 THEME_NAME:
 BASE_URL:

run:
  path: concourse-tasks/blog/generate.bash
